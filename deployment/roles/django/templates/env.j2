LOG_STDOUT=yes

SECRET_KEY="{{ secret_key }}"
ALLOWED_HOSTS={{ allowed_hosts|join(',') }}

DB_HOST={{ ansible_docker0.ipv4.address }}
DB_PORT=5432
DB_NAME={{ django_db.name }}
DB_USER={{ django_db.user }}
DB_PASSWORD={{ django_db.password }}
SENTRY_DSN={{ sentry_dsn }}

ELASTIC_SEARCH=http://172.17.0.1:9200/
ELASTIC_INDEX={{ elastic_index }}

EMAIL_HOST={{ smtp_host }}
DEFAULT_FROM_EMAIL={{ from_email }}

EMAIL_ORGANIST={{ emails.organist }}
EMAIL_BEAMIST={{ emails.beamist }}
EMAIL_KOSTER={{ emails.koster }}
EMAIL_PREACHER={{ emails.preacher }}
EMAIL_BIBLE_GROUP={{ emails.bible_group }}
EMAIL_PREACH_CREATION={{ emails.preach_creation }}
